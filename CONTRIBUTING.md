# 贡献指南

## 开发流程

1. 使用 Node.js 20.19+ 或 22.12+。
2. 运行 `npm install` 安装依赖。
3. 运行 `npm run dev` 启动本地开发服务器。
4. 提交前运行 `npm test`。

## 变更原则

- 保持本地优先和无后端的产品边界。
- 优先复用现有类型、表单、数据库和模板约定。
- 对数据结构变更同时更新 Zod schema、类型、默认值、导入兼容和测试。
- 对打印布局变更同时检查屏幕预览与 PDF。
- 保持键盘、移动端和辅助技术可用性。

## 隐私要求

- 不要提交真实姓名、手机号、邮箱、照片、学籍或证书。
- 不要提交 Resume Maker 导出的 JSON 备份。
- 测试数据使用明显的虚构内容。
- 私有开发资料放在已忽略的 `private/` 目录。

## Pull Request

PR 说明应包含变更目的、验证方式和必要的界面截图。截图必须使用虚构数据。
